import { LogEntryDetailsPipe } from './log-entry-details.pipe';

describe('LogEntryDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new LogEntryDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
